To run: python3 -m http.server 8000 in the folder → localhost:8000. Double-clicking works too, but file:// blocks cameras — the start screen detects that and points at keyboard mode.

Keyboard mode (no webcam needed, same engine): ←→ punch · Z X kick · ↓ squat · ↑ jump · Q E lean.